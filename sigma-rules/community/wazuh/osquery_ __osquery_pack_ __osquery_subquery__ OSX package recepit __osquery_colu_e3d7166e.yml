title: 'osquery: $(osquery.pack) $(osquery.subquery): OSX package recepit $(osquery.columns.package_id) $(osquery.columns.version) is installed on the system'
id: 8f9b416b-abd6-5f37-be15-1c62ec49f7a5
status: stable
level: low
description: 'osquery: $(osquery.pack) $(osquery.subquery): OSX package recepit $(osquery.columns.package_id) $(osquery.columns.version) is installed on the system'
author: Wazuh, Inc.
logsource:
  product: wazuh
detection:
  selection:
    osquery.name: package_receipts
  condition: selection

# Sigma+ Extensions (for advanced detection)
extensions:
  parent_rule: wazuh-24100
references:
  - https://github.com/wazuh/wazuh
  - https://documentation.wazuh.com/current/user-manual/ruleset/custom.html
custom_attributes:
  wazuh_rule_id: wazuh-24121
  wazuh_level: 4