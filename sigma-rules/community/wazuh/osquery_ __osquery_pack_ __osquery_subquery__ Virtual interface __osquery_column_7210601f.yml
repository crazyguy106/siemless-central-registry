title: 'osquery: $(osquery.pack) $(osquery.subquery): Virtual interface $(osquery.columns.name) with uuid $(osquery.columns.uuid) is on the system'
id: 5696d8c2-a29b-557b-8188-ea7b69effb47
status: stable
level: low
description: 'osquery: $(osquery.pack) $(osquery.subquery): Virtual interface $(osquery.columns.name) with uuid $(osquery.columns.uuid) is on the system'
author: Wazuh, Inc.
logsource:
  product: wazuh
detection:
  selection:
    osquery.name: ramdisk
  condition: selection

# Sigma+ Extensions (for advanced detection)
extensions:
  parent_rule: wazuh-24030
references:
  - https://github.com/wazuh/wazuh
  - https://documentation.wazuh.com/current/user-manual/ruleset/custom.html
custom_attributes:
  wazuh_rule_id: wazuh-24057
  wazuh_level: 4