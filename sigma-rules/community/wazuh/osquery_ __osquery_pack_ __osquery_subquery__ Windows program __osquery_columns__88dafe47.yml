title: 'osquery: $(osquery.pack) $(osquery.subquery): Windows program $(osquery.columns.name) $(osquery.columns.version) is installed on the system'
id: 051dac2c-5f38-5956-9830-0a08655f0d29
status: stable
level: low
description: 'osquery: $(osquery.pack) $(osquery.subquery): Windows program $(osquery.columns.name) $(osquery.columns.version) is installed on the system'
author: Wazuh, Inc.
logsource:
  product: wazuh
detection:
  selection:
    osquery.name: windows_programs
  condition: selection

# Sigma+ Extensions (for advanced detection)
extensions:
  parent_rule: wazuh-24100
references:
  - https://github.com/wazuh/wazuh
  - https://documentation.wazuh.com/current/user-manual/ruleset/custom.html
custom_attributes:
  wazuh_rule_id: wazuh-24119
  wazuh_level: 4