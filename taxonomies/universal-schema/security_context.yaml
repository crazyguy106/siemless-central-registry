category: security_context
version: 2.0.0
last_updated: '2026-01-01'
description: Security Context fields
fields:
  event_id:
    canonical: event_id
    aliases:
    - EventID
    - event_id
    - event.id
    - event.code
    - EventCode
    - event_identifier
    - log_id
    description: Event Id
  event_type:
    canonical: event_type
    aliases:
    - event.action
    - event.category
    - Category
    - EventCategory
    - action
    - event_type
    - event_name
    - log_type
    description: Event Type
  subject_user:
    canonical: subject_user
    aliases:
    - SubjectUserName
    - SubjectUser
    - initiating_user
    - actor_user
    - actor.user.name
    - user.name
    - source_user
    - initiated_by
    description: Subject User
  target_object:
    canonical: target_object
    aliases:
    - TargetObject
    - ObjectName
    - object_path
    - object_name
    - object.path
    - target_path
    - resource
    description: Target Object
  access_mask:
    canonical: access_mask
    aliases:
    - AccessMask
    - Mask
    - access.mask
    - permissions
    - AccessRights
    - access_rights
    - permission_mask
    - access_level
    description: Access Mask
