# Logstash Grok Filter for PALOALTO FIREWALL
# Generated by SIEMLess AI-Powered Schema Detection

filter {
  grok {
    match => { "message" => "" }
  }

  # Add vendor/product tags
  mutate {
    add_field => {
      "vendor" => "paloalto"
      "product" => "firewall"
    }
  }

  # Parse entity types
}
