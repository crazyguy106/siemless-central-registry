signatures:
- vendor: amazon web services
  product: cloudtrail
  log_type: audit
  version: null
  log_format: json
  signature_name: aws_cloudtrail_generic
  primary_pattern: '"eventSource":\s*"[a-z0-9.-]+\.amazonaws\.com"'
  confidence: 0.98
  priority: 5
  source: bulk_import
  upstream_sources:
  - json:aws-cloudtrail-signatures.json
